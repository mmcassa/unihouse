import { ScheduledChangeTypePipe } from './scheduled-change-type.pipe';

describe('ScheduledChangeTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ScheduledChangeTypePipe();
    expect(pipe).toBeTruthy();
  });
});
