import { ScheduledChangeTypeAppearancePipe } from './scheduled-change-type-appearance.pipe';

describe('ScheduledChangeTypeAppearancePipe', () => {
  it('create an instance', () => {
    const pipe = new ScheduledChangeTypeAppearancePipe();
    expect(pipe).toBeTruthy();
  });
});
