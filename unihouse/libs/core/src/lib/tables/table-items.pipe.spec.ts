import { TableItemsPipe } from './table-items.pipe';

describe('TableItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new TableItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
