import { EnvironmentTypePipePipe } from './environment-type-pipe-pipe';

describe('EnvironmentTypePipePipe', () => {
  it('create an instance', () => {
    const pipe = new EnvironmentTypePipePipe();
    expect(pipe).toBeTruthy();
  });
});
